#!/bin/bash

path=/vagrant-scripts/provision/ubuntu

$path/base
$path/ruby
$path/jenkins
$path/aws

$path/aws-user jenkins /var/lib/jenkins
sudo -u jenkins $path/vagrant
sudo -u jenkins $path/jenkins-user /vagrant/jobs
